<a href="{{ path ('app_addclub')}}">Add Club</a>
{#route fel controleur#}
<h4>List Clubs</h4>
{% for i in tabClubs %} //esm alias
    id:{{ i.id }}
    name:{{ i.name }}
    description: {{ i.description }}
    <a href="{{ path('app_updateClub',{'id':i.id}) }}">Update</a>
    <a href="{{ path('app_removeClub',{'id':i.id}) }}">Delete</a>
    <br>
{% endfor %}